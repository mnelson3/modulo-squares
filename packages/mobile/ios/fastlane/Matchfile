# Matchfile - Code signing configuration for Modulo Squares
git_url(ENV["MATCH_GIT_URL"])
git_branch("main")
storage_mode("git")
type("appstore")
username(ENV["FASTLANE_APPLE_ID"])
team_id(ENV["FASTLANE_TEAM_ID"])