services:
  github-runner:
    image: myoung34/github-runner:latest
    container_name: modulo-squares-runner
    env_file:
      - .env.runner
    environment:
      REPO_URL: https://github.com/mnelson3/modulo-squares
      RUNNER_NAME: modulo-squares-docker-runner-${HOSTNAME:-local}
      RUNNER_TOKEN: ${RUNNER_TOKEN}
      RUNNER_WORKDIR: /tmp/runner/work
      LABELS: self-hosted,ubuntu-latest,docker,modulo-squares
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - runner-work:/tmp/runner/work
      - /tmp:/tmp
    networks:
      - runner-network
    restart: unless-stopped

  # Optional: Runner with GPU support (uncomment if needed)
  # github-runner-gpu:
  #   image: myoung34/github-runner:latest
  #   container_name: modulo-squares-runner-gpu
  #   environment:
  #     REPO_URL: https://github.com/mnelson3/modulo-squares
  #     RUNNER_NAME: docker-runner-gpu-${HOSTNAME:-local}
  #     RUNNER_TOKEN: ${RUNNER_TOKEN}
  #     RUNNER_WORKDIR: /tmp/runner/work
  #     LABELS: self-hosted,ubuntu-latest,docker,modulo-squares,gpu
  #   volumes:
  #     - /var/run/docker.sock:/var/run/docker.sock
  #     - runner-work:/tmp/runner/work
  #     - /tmp:/tmp
  #   networks:
  #     - runner-network
  #   restart: unless-stopped
  #   deploy:
  #     resources:
  #       reservations:
  #         devices:
  #           - driver: nvidia
  #             count: 1
  #             capabilities: [gpu]

  # Monitoring container (optional)
  runner-monitor:
    image: nginx:alpine
    container_name: modulo-squares-runner-monitor
    ports:
      - "8082:80"
    volumes:
      - ./monitoring/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./monitoring/status.html:/usr/share/nginx/html/index.html:ro
    networks:
      - runner-network
    restart: unless-stopped
    depends_on:
      - github-runner

volumes:
  runner-work:
    driver: local

networks:
  runner-network:
    driver: bridge