name: Test Runner

on:
  push:
    branches:
      - develop
    paths:
      - '.github/workflows/test-runner.yml'

jobs:
  test:
    runs-on: [self-hosted, macos-latest]
    steps:
      - name: Test Runner
        run: echo "Runner is working!"